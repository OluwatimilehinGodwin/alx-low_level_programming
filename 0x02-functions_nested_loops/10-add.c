#include "main.h"

/**
 * add - entry point
 * @a: active variable
 * @b: second active variable
 *
 * Description: function that adds two integers
 * and returns the result
 * Return: 0
 */
int add(int a, int b);
{
int result = a + b;

return (result);
}
