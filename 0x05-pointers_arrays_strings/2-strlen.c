#include "main.h"

/**
 * _strlen - entry point
 * @s: active variable
 *
 * Description: function that returns the length of a string.
 * Return: 0
 */
int _strlen(char *s)
{
int x = (*s / 8);

return (x);
}
