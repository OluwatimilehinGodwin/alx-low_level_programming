#include <stdio.h>

/**
 * main - entry point
 *
 * Description: program to print the alphabeth in small letters
 * Return: 0
 */
int main(void)
{
int t = 0;

while (t < 10)
{
	printf("%i\n", t);
	t++;
}
return (0);
}
