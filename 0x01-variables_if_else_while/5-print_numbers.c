#include <stdio.h>

/**
 * main - entry point
 *
 * Description: program to print the alphabeth in small letters
 * Return: 0
 */
int main(void)
{
int t = 0;

while (t < 10)
{
	printf("%i", t);
	t++;
}
printf('\n');
return (0);
}
